# SubscriptionService
REST service for aggregation data about user online-subscriptions 

## How to run
Run ```docker-compose up --build -d``` to start all services

On ```localhost:8080/swagger/index.html``` you may see all documentation for API